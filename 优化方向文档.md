# 高级算法原理实践项目优化方向文档

更新时间：2026 年 9 月 22 日

## 一 优化目标

本项目首先必须完整满足任务书要求，然后在算法智能性、实际问题建模、可解释性、运行效率和交互体验方面形成明显区别。

最终目标不是单纯增加功能数量，而是让老师能够清楚看到：

1. 软件能够稳定完成任务书要求的全部基本功能。
2. 小组理解拓扑排序结果可能指数增长的问题，并设计了合理的处理策略。
3. 软件能够从有向关系中提取比“一个排序结果”更有价值的信息。
4. 扩展功能能够服务课程学习计划、项目任务和装配流程等真实场景。
5. 每个技术亮点都有测试、性能数据、界面展示和文档说明作为证据。

优化优先级统一为：

```text
正确性与稳定性 > 算法智能性 > 实际应用价值 > 可解释性 > UI 效果 > 功能数量
```

## 二 任务书基本要求保障

以下内容属于必须保留的基础能力，任何优化都不能破坏：

- [x] 本地桌面图形界面，不使用网页形式
- [x] 图形界面输入关系
- [x] TXT 文件导入
- [x] 按 `<前驱节点,后继节点>` 格式解析关系
- [x] 显示有向关系图
- [x] 输出尽可能多的拓扑排序结果
- [x] TXT 排序结果导出
- [x] PNG、SVG、PDF 关系图导出
- [x] 自环和普通有向环检测
- [x] 错误格式、重复关系和文件错误处理
- [x] 程序能力支持指导书示例和实际课程关系数据
- [x] 使用任务书图 1 的复刻数据完成导入、绘图和结果导出
- [x] 使用《2024版学业指南》明确先修要求核实的部分课程数据完成导入、绘图和结果导出
- [ ] 与资料负责人确认正式验收所需的完整课程范围
- [x] 提供运行说明、测试和技术文档

优化过程中必须持续运行基础回归测试，确保扩展功能不会影响以上内容。

## 三 当前已经具备的高分亮点

### 1 自适应多结果处理

- 能完成时枚举全部拓扑序。
- 结果过多时按照用户设置的上限停止展示。
- 明确区分“已完成全部枚举”和“达到上限未完全枚举”。
- 候选节点稳定排序，保证相同输入得到一致结果。

### 2 精确方案总数统计

- 18 个节点以内使用位掩码动态规划统计全部拓扑序数量。
- 精确统计不需要把所有结果保存到界面中。
- 节点数量较大时自动跳过指数级统计，保护响应速度。

当前机器单次性能抽测：

| 数据规模 | 处理策略 | 结果 | 耗时 |
|---|---|---:|---:|
| 18 个节点 | 精确动态规划 | 355687428096000 种 | 约 0.49 秒 |
| 19 个节点 | 自动跳过精确计数 | 展示前 1000 条 | 约 0.004 秒 |

以上数据只用于确认方案可行，正式报告应使用固定数据、固定环境和多次运行结果。

### 3 DAG 智能分析

- 自动生成可以并行执行的阶段。
- 计算最大并行宽度。
- 找出最长依赖链。
- 判断拓扑序是否唯一。
- 识别可以由其他路径推导出的传递冗余关系。
- 允许在关系图中隐藏冗余边，但不改变原始数据。

### 4 可解释关系探索

- 点击节点后高亮全部前置和后续节点。
- 显示节点的直接前驱和直接后继。
- 有环时返回并高亮一条具体环路，而不是只提示“排序失败”。

### 5 交互与视觉

- 分层、力导向和环形三种关系图布局。
- 深色与浅色主题。
- 排序结果与智能分析分标签显示。
- 后台线程执行计算，避免界面失去响应。
- 中文批处理一键安装、一键启动，并使用独立 `.venv`。
- 长链分行显示、长名称换行和高清图导出，修复测试反馈中的可读性问题。
- 多结果分页浏览，复制和导出仍保留本次生成的全部结果。

## 四 下一阶段确认的优化方向

任务书指定案例和培养方案中一组明确的先修关系已经完成验证。测试反馈指出的长链与长名称显示问题已修复；带容量限制的阶段规划已实现第一版。后续重点是算法过程解释、可重复性能实验，以及正式课程数据范围和人工测试复核。

### 前置任务 指定案例与真实数据闭环

优先级：必须首先完成

#### 目标

把“程序可以处理课程关系”变成能够提交和展示的实际证据。

#### 计划产出

- 任务书图 1 对应的关系 TXT 数据（已完成）。
- 一份从《2024版学业指南》核实的部分真实课程关系 TXT 数据（已完成；完整范围待确认）。
- 图 1 和已核实课程关系的关系图、拓扑排序结果均已导出。
- 自动检查每条 `<a,b>` 中 a 都位于 b 前面的验证结果。
- 可直接放入报告的界面截图。

#### 验收标准

- 两份 TXT 均能正常导入。
- 节点数和关系数与整理结果一致。
- 关系图方向正确、标签可读。
- 导出的每个拓扑序均满足全部先后关系。
- 资料中没有明确依据的课程关系不强行添加。

### 优化方向一 带容量限制的最短阶段规划

状态：核心算法、界面、阶段着色与结果导出已实现；仍需结合人工测试复核。

#### 目标

在普通并行分层基础上，允许用户设置“每个阶段最多安排 N 个节点”，自动生成阶段数量尽可能少的执行计划。

该功能保持通用：

- 节点代表课程时，可理解为每学期最多安排 N 门课程。
- 节点代表项目任务时，可理解为每阶段最多并行 N 项任务。
- 节点代表装配步骤时，可理解为每个工位周期最多处理 N 项工作。

#### 算法策略

- 小规模图：使用状态压缩搜索或动态规划求精确最少阶段数。
- 大规模图：使用最长依赖链优先的启发式算法快速生成建议。
- 界面必须明确标注“精确最优方案”或“启发式建议方案”。
- 所有输出必须满足原始先后依赖关系和阶段容量限制。

#### 计划产出

- 阶段容量输入控件。
- 最短阶段规划核心算法。
- 分阶段结果列表。
- 关系图按阶段着色。
- 精确方案与启发式方案说明。
- 正确性、边界和性能测试。

#### 验收标准

- 容量为 1 时，结果等价于一个合法拓扑序。
- 容量足够大时，结果不劣于普通并行分层。
- 每阶段节点数量不超过设定上限。
- 任意节点都不能早于其前驱节点所在阶段。
- 小规模数据能够通过穷举结果验证阶段数最优。

### 优化方向二 拓扑排序过程解释与回放

优先级：高

#### 目标

让软件能够解释一个拓扑序是如何产生的，而不只是显示最终结果。

#### 展示内容

- 当前步骤的零入度候选节点。
- 本步骤选择的节点。
- 受影响节点的入度变化。
- 新进入候选集合的节点。
- 当前已经形成的部分排序结果。

#### 计划产出

- 核心算法步骤记录结构。
- 上一步、下一步、自动播放和暂停操作。
- 关系图中当前节点和候选节点高亮。
- 算法过程文字说明。
- 与最终拓扑序一致性的自动化测试。

#### 验收标准

- 每一步记录都能由上一状态正确得到。
- 所有步骤播放完成后得到的结果与核心算法输出一致。
- 有环时停止播放，并展示无法继续的原因。
- 播放功能不修改原始关系数据。

### 优化方向三 性能实验与自适应策略证明

优先级：高

#### 目标

通过实验数据证明算法设计确实提高了效率，而不是只在报告中描述复杂度。

#### 对比内容

- 只求一个拓扑序与枚举多个拓扑序的耗时差异。
- 直接枚举计数与状态压缩动态规划计数的差异。
- 有结果上限与无保护枚举的差异。
- 精确阶段规划与启发式阶段规划的差异。

#### 计划产出

- 小型、中型和较大规模测试数据。
- 节点数、关系数、方案数、运行时间和处理策略记录。
- 性能对比表和趋势图。
- 可直接用于项目报告的复杂度与实验结论。

#### 验收标准

- 使用固定数据和固定随机种子，实验可以重复执行并得到相近结果。
- 测试数据和运行环境记录完整。
- 所有性能数字由程序测量，不手工编造。
- 每组关键测试运行 3 至 5 次，并记录平均值或中位数。
- 程序能够输出 CSV 数据和报告可用的性能图表。
- 报告能够解释程序为什么在不同规模下切换策略。

## 五 可选优化方向

以下内容只有在三项主要优化全部稳定后才考虑，不作为当前承诺范围。

### 1 关键节点影响分析

- 统计一个节点影响的全部后续节点数量。
- 判断节点是否位于最长依赖链上。
- 模拟删除节点或关系后，阶段数和方案数如何变化。

### 2 多种方案排序

- 优先展示关键课程较早完成的方案。
- 优先展示阶段负载更加均衡的方案。
- 允许用户按自定义优先级对可行方案重新排序。

### 3 项目状态保存

- 保存当前输入、结果上限、布局和主题。
- 下次启动时恢复上次工作状态。

## 六 明确不做的方向

为避免偏离任务书和无法按时完成，当前不做：

- 不增加服务器、账号、联网同步和网页版本。
- 不为了显得高级而加入与拓扑排序无关的人工智能或机器学习。
- 不将软件写死为某一份培养方案。
- 不自行增加学分、成绩、选修规则等资料中不存在的条件。
- 不承诺对大规模图无限制枚举全部拓扑序。
- 不在核心功能和测试稳定前增加大量装饰动画。
- 不同时维护多套功能相同的算法代码而缺少对比目的。

## 七 与验收评分点的对应关系

| 任务书评分点 | 对应证据 |
|---|---|
| 设计文档完整性 | 需求、设计、技术说明、测试清单、优化方向和性能实验文档 |
| 原创性 | 自行实现核心算法、智能分析、测试代码和连续 Git 提交记录 |
| 可运行 | 独立 `.venv`、双击安装和启动、启动回归测试 |
| 功能完整 | 输入、导入、关系图、多结果、导出和异常处理 |
| 功能扩充 | 精确计数、阶段规划、关键链、冗余关系、过程回放 |
| 代码规范 | 类型标注、模块划分、Ruff 和自动化测试 |
| 智能性 | 数据规模自适应策略、动态规划、约束规划和性能证明 |

## 八 最终高分版本目标

最终技术版本应形成以下完整故事：

```text
通用关系输入
  ↓
格式验证和环检测
  ↓
拓扑序枚举与自适应结果保护
  ↓
精确方案计数与唯一性判断
  ↓
并行阶段、最长依赖链和冗余关系分析
  ↓
带容量限制的最短阶段规划
  ↓
算法步骤解释与关系图交互展示
  ↓
真实课程数据验证与性能实验
```

建议最终只重点讲三个核心创新：

1. 小规模精确计算、大规模自动保护的自适应求解策略。
2. 从普通拓扑排序扩展到带容量限制的最短阶段规划。
3. 可解释算法过程和可验证的性能实验。

## 九 答辩表达建议

可以使用下面的表述介绍技术亮点：

> 我们没有停留在输出一个拓扑序，而是针对多结果可能指数增长的问题设计了自适应策略。小规模图使用状态压缩动态规划精确统计全部方案，大规模图自动采用结果上限保护。系统还能分析并行阶段、最长依赖链、唯一性和冗余关系，并扩展出带容量限制的阶段规划：小图求精确最优，大图给出明确标注的启发式建议。后续将用算法过程回放和性能实验进一步验证设计效果。

答辩时应展示：

- 一个普通多结果示例。
- 一个带环错误示例。
- 一个方案总数远大于界面展示上限的示例。
- 一个存在冗余关系的示例。
- 一个带阶段容量限制的规划示例。
- 一张性能对比表。

## 十 方向确认清单

- [x] 基本功能完整性优先
- [x] 保持通用拓扑排序应用定位
- [x] 已完成精确计数和 DAG 智能分析
- [x] 已完成交互关系图和深浅主题
- [x] 补齐任务书图 1 的验证证据
- [x] 补齐部分真实课程数据的验证证据
- [ ] 与资料负责人确认正式课程数据的完整范围
- [x] 实现带容量限制的最短阶段规划第一版
- [x] 修复长链和超长名称关系图的可读性问题
- [ ] 随后实现算法过程解释与回放
- [ ] 最后完成性能实验和报告材料
- [ ] 三项主要优化完成后停止扩大技术范围
